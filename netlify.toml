# Static site — no build step. Netlify just publishes the repo root.
[build]
  publish = "."
  command = ""

# Send the bare domain to the login page.
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
